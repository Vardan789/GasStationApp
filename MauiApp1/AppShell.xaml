<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:MauiApp1"
       Shell.FlyoutBehavior="Flyout"
       Title="MauiApp1"
       x:Class="MauiApp1.AppShell">  <!-- Ensure this matches the namespace and class name -->

    <!-- Flyout Menu (Burger Menu) -->
    <FlyoutItem Title="Home">
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage"/>
    </FlyoutItem>

    <FlyoutItem Title="QR History">
        <ShellContent Title="QR History" ContentTemplate="{DataTemplate local:MyQrHistory}" Route="QRHistoryPage"/>
    </FlyoutItem>

</Shell>