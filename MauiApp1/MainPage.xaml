<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             BackgroundColor="Black"
             Title="Home">

    <Grid>
        <!-- Background Image -->
        <Image Source="shell.jpg"
               Aspect="AspectFill"
               Opacity="0.3"
               IsVisible="True"/>

        <!-- Foreground Content -->
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="30">

                <!-- Slider Bar (Product Pills) -->
                <ScrollView Orientation="Horizontal"
                            HorizontalScrollBarVisibility="Never"
                            HeightRequest="100">
                    <FlexLayout Direction="Row"
                                AlignItems="Center"
                                BindableLayout.ItemsSource="{Binding Products}"
                                Wrap="NoWrap"
                                Padding="10,0">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <Border Stroke="#444"
                                        WidthRequest="100"
                                        HeightRequest="80"
                                        StrokeThickness="2"
                                        Background="#1a1a1a"
                                        StrokeShape="RoundRectangle 20"
                                        Padding="10"
                                        Margin="10,0">
                                    <VerticalStackLayout Spacing="2" VerticalOptions="CenterAndExpand">
                                        <Label Text="{Binding Title}"
                                               TextColor="White"
                                               FontAttributes="Bold"
                                               FontSize="14"
                                               HorizontalTextAlignment="Center"/>
                                        <Label Text="{Binding Price, StringFormat='${0:F2}'}"
                                               TextColor="Gray"
                                               FontSize="12"
                                               HorizontalTextAlignment="Center"/>
                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </FlexLayout>
                </ScrollView>

                <!-- Stations Header -->
                <Label Text="Nearby Stations"
                       FontSize="22"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center" />

                <!-- Stations Grid -->
                <CollectionView ItemsSource="{Binding Stations}"
                                SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical"
                                         Span="4"
                                         HorizontalItemSpacing="30"
                                         VerticalItemSpacing="30"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Stroke="#444"
                                    Background="#1a1a1a"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 20"
                                    Padding="5"
                                    Margin="5">
                                <VerticalStackLayout Spacing="10"
                                                     HorizontalOptions="Center"
                                                     WidthRequest="150"> <!-- Adjust width as needed -->
                                    <Image Source="{Binding Image}"
                                           WidthRequest="105"
                                           HeightRequest="105"
                                           Aspect="AspectFill"/> <!-- AspectFill added here -->
                                    <Label Text="{Binding Title}"
                                           FontSize="15"
                                           FontAttributes="Bold"
                                           HorizontalTextAlignment="Center"
                                           TextColor="White"/>
                                    <Label Text="{Binding Address}"
                                           FontSize="10"
                                           HorizontalTextAlignment="Center"
                                           TextColor="#CCCCCC"/>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
